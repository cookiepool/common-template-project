/*
 * 这个文件用来生成动态路由（不同的用户有不同的权限）
 */

const state = {
  routes: [],
  addRoutes: []
};

const mutations = {};

const actions = {};

export default {
  namespaced: true,
  state,
  mutations,
  actions
};
